octave 数学软件
入门：
    每个命令后面加';'可以让命令执行不回显

命令备忘：
 # 生成随机的3x2阵列
 a=rand(3,2);

公开函数：函数名字与文件名字相同
与函数名不同的函数只能被同文件中的函数调用
放在private目录下的函数文件是私有函数

可变返回变量varargout,nargout,varargin,用于定义函数可可变参数，可变返回

文件除了注释，第一次出现的是function,此文件则定义为function文件，否则会被认为是脚本(script)文件
在一个脚本中执行另外一个脚本有souce和直接写文件名

当自定义的函数把内置函数覆盖(overload),可以用builtin函数来只能内置函数调用

函数锁mlock mislock munlock

savepath 把添加的路径保存到指定文件(缺省~/.octaverc)中

函数能够被引用例如
f = @sqrt

f(4)
-> 2
* 乘法，矩阵的乘法必须要阶符合要求
.* 元素对应乘法 单独元素对应乘法

linspace(lo,hi,cnt) lo下限,hi上限,cnt-1被划分份
例如：linspace(-8,8,40) 从-8到+8，分成39份

eps 
更准确地说，eps 是机器浮点系统中任意两个相邻数字之间的相对间距。该数值既取决于系统本身， 
也取决于该数字在浮点系统可表示范围内的具体位置。在支持 IEEE 754 浮点运算的机器上，对于双 
精度浮点数，eps(1.0) 大约是 2.2204e-16；对于单精度浮点数，大约是 1.1921e-07 1.0在系统

1.0 其实是以一个在[1.0-eps(1.0),1.0+eps(1.0)]的范围到值
